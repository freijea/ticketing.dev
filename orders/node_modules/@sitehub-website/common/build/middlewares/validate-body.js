"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateBody = void 0;
const express_validator_1 = require("express-validator");
const validations = {
    email: (0, express_validator_1.body)('email')
        .isEmail()
        .withMessage('Email must be valid'),
    passwordSignup: (0, express_validator_1.body)('password')
        .trim()
        .isLength({ min: 4, max: 20 })
        .withMessage('Password must be between 4 and 20 characters'),
    passwordSignin: (0, express_validator_1.body)('password')
        .trim()
        .notEmpty()
        .withMessage('You must provide a password')
};
const validateBody = (validationRequest) => {
    let validationArray = [];
    for (let [key, value] of Object.entries(validations)) {
        if (validationRequest.indexOf(key) > -1) {
            validationArray.push(value);
        }
    }
    return validationArray;
};
exports.validateBody = validateBody;
